# --- Python ---
__pycache__/
*.pyc
*.pyo
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/

# --- legacy code/data (superseded, not tracked) ---
legacy/

# --- checkpoints: no weights are tracked ---
#     regenerate with the training scripts; paths listed in docs/version.md Appendix A
runs/

# --- instances: no datasets are tracked ---
#     regenerate with proposed_src/util/generate_hard_instances.py and
#     proposed_src/v22_transfer/v22_make_testset.py; paths in docs/version.md Appendix A
instances/
!instances/HARD_INSTANCES_README.md

# --- misc ---
*.log
texput.log
.DS_Store

# --- LaTeX build artifacts (PDFs are tracked, intermediates are not) ---
*.aux
*.out
*.toc
*.synctex.gz
*.fls
*.fdb_latexmk
